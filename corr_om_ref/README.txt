First compile the project with cmake and make
Then add 2 types of runs in the ref_om_correction.sh:
  - 1 pdf run (high statistics run for the references OMs often ~ 12 hours) in variable pdf_num
  - Severals references OMs which are ~ 15 min per day in variable ref_num

source ref_om_correction.sh
